node_modules/
.DS_Store
*.log
.firebase/
.firebaserc

# Scaffolding from a stray `firebase init` — the repo already ships a
# configured firebase.json, so init only ever adds a duplicate codebase.
/Surplus Donation Platform/
.firebase/

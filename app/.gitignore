logs
*.log
npm-debug.log*
node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Environment files — never commit real keys
.env
.env.local
.env.*.local

# Env files, including misnamed variants (env.local without the leading dot
# is invisible to Vite but was NOT caught by the pattern above).
env.local
env.*.local
*.env.local

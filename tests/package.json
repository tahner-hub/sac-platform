{
  "name": "sac-tests",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Security-rules tests for firestore.rules, run against the Firestore emulator.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.18.0",
    "vitest": "^3.2.4"
  }
}
